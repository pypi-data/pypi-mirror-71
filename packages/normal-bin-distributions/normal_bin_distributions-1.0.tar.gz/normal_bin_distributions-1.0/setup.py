from setuptools import setup

setup(name='normal_bin_distributions',
      version='1.0',
      description='Binomial and Gaussian distributions',
      packages=['normal_bin_distributions'],
      author = 'Victor Emenike',
      zip_safe=False)
