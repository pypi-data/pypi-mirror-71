import os
import sys

try:
    from setuptools import setup
except ImportError:
    from distutils.core import setup

requires = []

setup(
    name = "lambdatesting2323232323",
    packages = ["src"],
    version = '0.1',
    description = 'Example inherits package',
    #url = "",
    #download_url = "",
    author = "FMU",
    classifiers = [],
)