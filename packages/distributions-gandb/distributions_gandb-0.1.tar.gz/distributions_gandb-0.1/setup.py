from setuptools import setup

setup(name='distributions_gandb',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_gandb'],
      author='Siddhesh More',
      zip_safe=False)
