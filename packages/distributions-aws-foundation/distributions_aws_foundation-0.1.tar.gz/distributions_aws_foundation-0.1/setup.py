from setuptools import setup

setup(name='distributions_aws_foundation',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_aws_foundation'],
      zip_safe=False)
