from setuptools import setup

setup(name='sampler_distributions_pkg',
      version='0.1',
      description='Gaussian distributions',
      packages=['sampler_distributions_pkg'],
      author='Daniel Bailey',
      zip_safe=False)
