from setuptools import setup

setup(name='distributions_gb_yr',
      version='1.0',
      description='Gaussian distributions',
      packages=['distributions_gb_yr'],
      author='Yash',
      zip_safe=False)
