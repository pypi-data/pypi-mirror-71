from setuptools import setup

setup(name='sid-distributions',
      version='0.1',
      description='Gaussian distributions and Binomial distributions',
      packages=['sid-distributions'],
      author=['Sidhartha Roy'],
      zip_safe=False)
