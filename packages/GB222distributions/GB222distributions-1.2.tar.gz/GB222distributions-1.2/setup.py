from setuptools import setup

setup(name='GB222distributions',
      version='1.2',
      description='Gaussian and Binomial distributions',
      packages=['GB222distributions'],
      author= 'bayode',
      email='bayfag@yahoo.com',
      zip_safe=False)
