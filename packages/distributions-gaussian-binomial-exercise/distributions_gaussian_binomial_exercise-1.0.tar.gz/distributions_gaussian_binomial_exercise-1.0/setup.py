from setuptools import setup

setup(name='distributions_gaussian_binomial_exercise',
      version='1.0',
      description='Gaussian distributions and Binomial distributions',
      packages=['distributions_gaussian_binomial_exercise'],
      author='JL',
    zip_safe=False)
