from setuptools import setup

setup(name='statsx_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['statsx_distributions'],
      zip_safe=False)
