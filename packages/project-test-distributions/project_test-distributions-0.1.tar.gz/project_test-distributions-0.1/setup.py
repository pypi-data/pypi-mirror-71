from setuptools import setup

setup(name='project_test-distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['project_test-distributions'],
      zip_safe=False)
