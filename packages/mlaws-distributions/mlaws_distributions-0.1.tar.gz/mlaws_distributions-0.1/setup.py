from setuptools import setup

setup(name='mlaws_distributions',
      version='0.1',
      description='Gaussian & Binomial distributions',
      packages=['mlaws_distributions'],
      author='Mohamed Zeglam',
      zip_safe=False)
