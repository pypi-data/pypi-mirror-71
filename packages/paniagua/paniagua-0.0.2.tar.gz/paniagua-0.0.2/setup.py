from setuptools import find_packages, setup
setup(
   name='paniagua', ## Este nombre debe ser único y no estar indexado
   version='0.0.2',
   packages=find_packages(),
   py_modules=['paniagua'] ## Este debe ser el nombre del archivo *.py
)
