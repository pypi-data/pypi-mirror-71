from setuptools import setup

setup(name='statistics_distributions',
      version='0.1',
      description='Gaussian Distribution and Binomial Distribution',
      packages=['statistics_distributions'],
      zip_safe=False)
