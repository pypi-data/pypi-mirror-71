from setuptools import setup

setup(name='tglmsm_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['tglmsm_distributions'],
      zip_safe=False)
