from setuptools import setup

setup(name='binomial-gaussian-distribution-basics',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['binomial-gaussian-distribution-basics'],
      zip_safe=False)
