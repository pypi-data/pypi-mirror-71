from setuptools import setup

setup(name='distributions_outlierd',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_outlierd'],
      author='Deivid',
      zip_safe=False)
