from setuptools import setup

setup(name='ktk_distributions',
      version='0.1',
      description='Gaussian distributions,Binomial distributions',
      packages=['ktk_distributions'],
      author='Kartikey Singh',
      zip_safe=False)
