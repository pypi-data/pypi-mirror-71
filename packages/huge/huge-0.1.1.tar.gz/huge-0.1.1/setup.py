#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['tqdm==4.46.1', 'click==7.1.2', 'tabulate==0.8.7']

entry_points = \
{'console_scripts': ['huge = huge:main']}

setup(name='huge',
      version='0.1.1',
      description='Find huge additions in Git history.',
      author='Radovan Bast',
      author_email='radovan.bast@uit.no',
      url='https://github.com/bast/huge',
      py_modules=['huge'],
      install_requires=install_requires,
      entry_points=entry_points,
     )
