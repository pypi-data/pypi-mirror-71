CLI_CONFIG = {}
CONFIG = {
    "be_verbose": {
        "dyne": "idem",
        "action": "store_true",
        "help": "List full instance information rather than just names",
        "default": False,
        "type": bool,
    }
}
SUBCOMMANDS = {}
DYNE = {
    "acct": ["acct"],
    "exec": ["exec"],
    "states": ["states"],
}
