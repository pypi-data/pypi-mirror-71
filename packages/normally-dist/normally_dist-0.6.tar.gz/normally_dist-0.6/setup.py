from setuptools import setup

setup(name='normally_dist',
      version='0.6',
      description='Gaussian and Binomial distributions',
      packages=['normally_dist'],
      author = 'Ifenna',
      zip_safe=False)
