from setuptools import setup

setup(name='distributionsMM',
      version='1.0',
      description='distributions Gaussian and Binomial',
      packages=['distributionsMM'],
      author='MM',
      zip_safe=False)
