from setuptools import setup

setup(name='probability-distributions-dsnd-aws',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['probability-distributions-dsnd-aws'],
      zip_safe=False)
