from setuptools import setup

setup(name='test_distribution',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['test_distribution'],
      zip_safe=False)
