from setuptools import setup

setup(name='dmgs_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['dmgs_distributions'],
      author='Daniel Guatibonza',
      zip_safe=False)
