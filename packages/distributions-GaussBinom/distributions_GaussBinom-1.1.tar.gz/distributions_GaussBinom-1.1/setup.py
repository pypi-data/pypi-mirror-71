from setuptools import setup

setup(name='distributions_GaussBinom',
      version='1.1',
      description='Gaussian distributions and Binomial distributions',
      packages=['distributions_GaussBinom'],
      zip_safe=False)
