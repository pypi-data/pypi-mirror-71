from setuptools import setup

setup(name='statistics_distributions_bg',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['statistics_distributions_bg'],
      zip_safe=False)
