from setuptools import setup

setup(name='racm_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['racm_distributions'],
      author='Raquel Cerda',
      zip_safe=False)
