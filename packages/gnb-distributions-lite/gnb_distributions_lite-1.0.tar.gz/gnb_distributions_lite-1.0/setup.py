from setuptools import setup

setup(name='gnb_distributions_lite',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['gnb_distributions_lite'],
      zip_safe=False)
