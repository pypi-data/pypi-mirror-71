from setuptools import setup

setup(name='binom_gaussian',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['binom_gaussian'],
      author='Author',
      author_email='author@website.com',
      zip_safe=False)
